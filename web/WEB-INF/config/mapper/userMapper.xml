<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.linqiaoli.dao.UserDao">
    <insert id="save">
        INSERT INTO USER(user_id,name) VALUES (#{userId},#{name});
    </insert>
</mapper>